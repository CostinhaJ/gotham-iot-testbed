requests
docker
graphviz

# Used by src/tese (capture_utils.py) to parse captured TLS handshakes.
# Also requires the `tshark` binary (Wireshark's CLI) installed on the
# host running the experiment -- not pip-installable, see src/tese/README.md.
pyshark
